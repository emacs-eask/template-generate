default:
  image: node:16

pages:
  stage: test
  before_script:
  - npm install @emacs-eask/cli -g
  - apt-get update
  - apt-get install emacs -y
  script:
  - eask clean all
  - eask package
  - eask install
  - eask compile
  only:
  - main
